version: '3'

services:
  redmine-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5050:5050"
    env_file:
      - credentials.yaml
    environment:
      - SERVER_PORT=5050
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
